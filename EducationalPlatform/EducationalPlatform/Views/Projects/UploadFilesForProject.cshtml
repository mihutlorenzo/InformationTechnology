@model EducationalPlatform.Models.Project
@{
    ViewBag.Title = "UploadFilesForProject";
}

<div>
    @* Make sure the enctype is multipart/form-data *@
    @using (Html.BeginForm("SaveFileToDatabase", "Projects", new { id = Model.Id }, FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        <p> Upload File</p>
        <input type="file" id="file" name="file" /><br />
        <input type="submit" value="Upload" />
    }
</div>

